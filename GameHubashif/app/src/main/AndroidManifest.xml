<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.GameHubAshif">

        <!-- Splash (Launcher) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main -->
        <activity android:name=".MainActivity" android:exported="true"/>

        <!-- Games -->
        <activity android:name=".TicTacToeActivity"/>
        <activity android:name=".MemoryGameActivity"/>
        <activity android:name=".SnakeGameActivity"/>
        <activity android:name=".QuizGameActivity"/>
        <activity android:name=".RunnerGame"/>

    </application>

</manifest>